import React from 'react'

import AppShell from '@components/AppShell'

const Stages = () => {
  return (
    <AppShell pageTitle="Estágio">
      <div>
        ESTAGIOS
      </div>
    </AppShell>
  )
}

export default Stages
